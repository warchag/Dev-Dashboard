<div align="center">
  <img src="build/sceen.png" alt="Dev Dashboard Logo" width="120" height="120">
  
  # 💻 Dev Dashboard
  
  **Ultimate Command Center for Developers**
  
  แอปพลิเคชันจัดการระบบสำหรับนักพัฒนาสายโหด รวมเครื่องมือที่สำคัญไว้ในที่เดียว ทั้งดูสถานะระบบ, จัดการพอร์ต, คุม Docker, มินิทูลส์ และ**เช็ค Network (Ping)** ครบจบในแอปเดียว! สร้างด้วยเทคโนโลยี **Electron + React + Vite** ภายใต้ดีไซน์แบบ **Glassmorphism** สุดล้ำ
  
  ---
</div>

## ✨ ฟีเจอร์หลัก (Features)

### 📊 System Overview (ภาพรวมระบบ)
- **CPU & RAM Monitor:** แสดงสถานะการทำงานของขุมพลัง CPU และหน่วยความจำแบบ Real-time พร้อมกราฟวงกลมสุดเท่
- **Disk Usage:** เช็คพื้นที่เต็ม / คงเหลือของฮาร์ดดิสก์หลัก
- **Network Stats:** แสดงความเร็วดวงน์โหลด/อัพโหลดปัจจุบัน พร้อมเช็ค Local IP และดึง **External IP** ให้คุณทันที

### 🔌 Ports & Processes (จัดการพอร์ต)
- เบื่อไหมกับการต้องมานั่งพิมพ์คำสั่ง `lsof -i` แล้วตามไป kill?
- ฟีเจอร์นี้จะ **สแกนพอร์ตทั้งหมดที่เปิดอยู่** (Listen Ports) แล้วแสดงผลเป็นตารางที่อ่านง่าย
- มาพร้อมช่องค้นหาพอร์ต / ชื่อโปรแกรม
- มีปุ่ม **Kill Process** แบบคลิกเดียวจบ (พร้อมระบบป้องกันกดพลาด Confirmation Modal) 💥

### 🐳 Docker Manager (ตัวคุม Docker)
- ตรวจสอบสถานะ Container ในเครื่องของคุณได้ทั้งหมด
- ไฟสเตตัสสีเขียว-แดงดูง่าย (Running / Stopped)
- แสดง Image, Port Mapping อย่างครบถ้วน

### 📄 Container Logs (ดู Log ทะลุจอ)
- เลือกระบุ Container ที่กำลังทำงานอยู่ แล้วดึง Log ออกมาอ่านแบบสดๆ
- มีระบบ Auto-Scroll ไหลตาม Log ใหม่ล่าสุดโดยอัตโนมัติ ไม่ต้องพิมพ์ `docker logs -f` อีกต่อไป

### 🌐 Network & Ping (ตรวจสอบเครือข่าย)
- **Local Network:** ปิงเข้า `localhost` หรือระบุ IP ในวง LAN (เช่นวงเครื่อง Router) แบบด่วนๆ เพื่อเช็คว่า Service ภายในยังรันอยู่หรือไม่
- **External Network:** เช็คเน็ตออกนอกด้วย Google (`8.8.8.8`) หรือ Cloudflare (`1.1.1.1`) แบบคลิกเดียว
- แสดงความเร็วเฉลี่ย (Avg Latency) และ % Packet loss ออกมาเป็นตัวเลขสถิติให้ดูง่ายโดยไม่ต้องแกะ Log เอง พร้อมมี Black Terminal ให้อ่าน Log ดิบระหว่าง Ping

### 🛠️ Mini Tools (เครื่องมือเข้ารหัสฉบับกระเป๋า)
- **JSON Formatter:** วาง JSON ที่เละเทะ แล้วกดปุ่มเดียวเรียงสวยงาม
- **Base64 Encode/Decode**
- **URL Encode/Decode**
- **JWT Decoder:** แกะข้อมูล Token ดู Header / Payload ได้ทันทีจากในแอป ไม่ต้องต่อเน็ต

### ⚙️ Settings (ปรับแต่งธีม)
- **Theme Color:** เปลี่ยนสีสันของแอปได้ดั่งใจ (Default Blue, Cyber Purple, Matrix Green, Sunset Orange, Synthwave Pink)
- **Font Family:** เลือกระหว่าง Inter, Roboto, และโค้ดเดอร์ฟอนต์อย่าง Fira Code
- ระบบจำค่าอัจฉริยะ ปิดเปิดใหม่สียังอยู่ครบ!

---

## 🚀 เทคโนโลยีที่ใช้ (Tech Stack)

* **[Electron](https://www.electronjs.org/)** - สำหรับทำ Desktop App ข้ามแพลตฟอร์ม
* **[React](https://reactjs.org/)** - สร้าง User Interface อันสวยงาม
* **[Vite](https://vitejs.dev/)** - ตัว Build Tool ที่เร็วแรงทะลุนรก
* **[TypeScript](https://www.typescriptlang.org/)** - เพิ่มความปลอดภัยและง่ายต่อสเกลโค้ด
* **Vanilla CSS** - ดีไซน์กราฟิกสไตล์ Glassmorphism ล้วนๆ

## 🛠 การติดตั้งและใช้งาน (Installation)

1. **โคลนโปรเจกต์ (Clone the repository)**
   ```bash
   git clone <your-repo-url>
   cd dev-dashboard
   ```

2. **ติดตั้ง Dependencies**
   ```bash
   npm install
   ```

3. **รันสถานะจำลอง (Development Mode)**
   ```bash
   npm run dev
   ```

4. **สร้างไฟล์เพื่อใช้งานจริง (Build Application)**
   - สำหรับ **Mac (Arm64/Intel)**
     ```bash
     npm run build:mac
     ```
   - สำหรับ **Windows**
     ```bash
     npm run build:win
     ```
   
   *ไฟล์ที่เสร็จแล้วจะอยู่ในโฟลเดอร์ `dist`*

---
<div align="center">
  <p><b>พัฒนาโดย <a href="https://www.facebook.com/learntodeveloper">เขียนโค้ดยามว่าง</a> ☕️</b></p>
</div>
